/**
 * Input ports defining the contracts for application use cases.
 * These interfaces are implemented by the application layer and called by the infrastructure layer.
 * They define what the application can do — not how it does it.
 */
package com.challenge.hotel.application.port.input;